# Build output (target-worktrees is the cargo target directory every agent worktree shares)
/target/
/target-worktrees/
/web/node_modules/
/web/dist/

# Local state: run artifacts, the local catalog database, worker logs, drafts and compiled engines
/artifacts/
/data/ui/
/strategy_workspace/

# Machine-specific configuration (see local.example.toml)
/local.toml

# Misc
*.part
.DS_Store

# Agent worktrees (see docs/LOOP.md)
/.claude/worktrees/
