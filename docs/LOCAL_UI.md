# Tessera local UI

The planned separation between the open-source application, external strategy packages, and
external data libraries is recorded in [OPEN_SOURCE_ARCHITECTURE_DIRECTION.md](OPEN_SOURCE_ARCHITECTURE_DIRECTION.md).
Proposed features and their acceptance criteria are tracked in [PRODUCT_BACKLOG.md](PRODUCT_BACKLOG.md).

Double-click `Launch Tessera.command` in Finder. The first launch builds the console bundle (`web/dist`) and the Rust service; later launches reuse both and open `http://127.0.0.1:8787/`. The console is a static single-page bundle served by the Rust service on the same origin as the API, so there is one process and no Node runtime once it is built. For UI work, `npm run dev` in `web/` serves the bundle on 5173 with hot reload and proxies `/api` to the running service.

The initial vertical slice provides:

- A local SQLite catalog at `data/ui/tessera_ui.sqlite3`.
- Automatic, non-destructive import of every `artifacts/*/report.html` result as an immutable legacy run.
- Every one-file SDK strategy discovered by `build.rs` (bundled examples plus any folders listed in `local.toml` `[strategies] dirs`), each with its manifest parameters exposed as form fields. Legacy catalog rows imported from older artifacts remain visible for their run history but are not runnable.
- A Strategies catalog rendered as a dense monitor table instead of tiles, so it scales to a large number of entries and reads as a scoreboard. Nine columns: #, strategy (the name, with its version and status as badges under it), assets, runs, CAGR %, Sharpe, max DD %, last run, and open. The metrics and the date are the strategy's last completed run's cached metrics (`metrics_json` on `runs`), carried on each row of `GET /api/dashboard` as `last_run` beside `run_count`; a strategy with no completed run, or whose last completed run has no cached metrics, shows a dash. Rows are grouped by asset class, status, or shown flat; custom releases nest under their base strategy; every column but open sorts (dashes always last); a filter box matches name, id, asset, or description; arrow keys move the selection and Enter opens it. A sticky inspector rail shows the selected strategy's description, config, base strategy, source hash, run count and last completed run, and Open / View source actions.
- An isolated two-worker queue.
- A strategy page that is a summary and a Run button over its history (decision 0003): the hero (name, badges, View source code, Configure run), the Production rules folded closed with their count on the bar, a summary strip of the current configuration (research label, window, universe, bars, capital, size, costs, entry limits, every strategy parameter with its value, and the preset it came from, `· edited` once a field changed) with Configure run and Run, and the Historical runs table directly under it. Configure run opens the run form in a modal dialog, the console's one (a native `dialog` element in the terminal look): four labelled rows (Identity: research label, dates, run name, cost profile; Data: universe or explicit symbols with no cap on their number, bar resolution, session, position size, minimum entry price, capital, warm-up; Limits: entries per day, open positions, maximum gross exposure as buying power, tie-break, seed; Parameters: the manifest parameters, the advanced tier behind the Simple/Advanced toggle), the presets (apply one, save the current settings as one) inside it, Cancel and Run. Escape, Cancel, or the close button keeps the edits in memory until the page changes; Run queues the run and closes the dialog. The strip's Run sends exactly the configuration the strip shows, the body the dialog's Run would send with no edits. The whole form shows without scrolling at 1280×800 in both display modes for a strategy declaring up to eight parameters, the symbol picker open: the dialog's head and foot are as dense in modern mode as in terminal mode, and a hint under a control is clamped at two lines with the full text as its tooltip (a 92 px numeric track wraps a sentence to five); past eight parameters the rows scroll inside the dialog.
- Runs that would not fit in memory (intraday bars across a whole universe for years) fail within seconds with the engine's estimate and suggested alternatives instead of swapping; failed jobs show the engine's own error message.
- Data sanitation on the run page: the Overview tab says how many off-calendar rows and one-bar spikes the load dropped and how many symbols it skipped (from `sanitation.json` beside the report), or that nothing was dropped; the Symbols tab lists every skipped symbol with its reason, and its tab label counts them.
- Tick lake support when `lake_dir` is configured: `EXCHANGE:SYMBOL` instruments appear in the picker (asset class Crypto, a `tick` chip), the run form offers 1s/5s/15s/30s bars for them, and strategies receive order-book features on every bar.
- A Studies page: pick a grid (lake seconds, or daily / 5-minute / 1-minute CSV bars, with symbols picked from the instrument catalog through the run form's picker, filtered to instruments with bars at that resolution; order-book features are hidden on CSV grids and the OHLCV set is pre-ticked, so nothing comes back unavailable), instruments, features (the bar bases as checkboxes, then a checkbox per registered series from `GET /api/studies/series`: every `[[data.series]]` entry in `local.toml` with its kind and availability rule, and the lake feeds funding_rate, funding_annualized, open_interest, and open_interest_usd, greyed out on CSV grids; all joined as-of availability), horizons, target, mode (time-series or cross-sectional), and decision delay; `agg daily` features on the daily grid lift from the intraday library, and every registered event series gets an event-path table; the selected cell shows its diagnostics (incremental IC against the accepted set, the per-day IC strip, regime table, autocorrelation); a feature library panel keeps named expressions in the catalog database (`GET`/`POST /api/features`, `POST /api/features/{id}/promote`, `DELETE /api/features/{id}`; a fresh catalog starts with the vol set, rv 5s, rv 30s, rv 60s, the 5s/60s vol ratio, and vol of vol 60s, seeded once by name so edits and deletions stick): "use" drops one into the candidates, "promote" puts it in the accepted set that every study regresses candidates against (a promoted feature that cannot run on the chosen grid is left out of that study), and the selected cell in the results can be promoted in place; a study with an accepted set also writes `accepted.parquet` (symbol, bar close, date, one column per accepted feature, one target column per horizon), downloadable from the results strip (`GET /api/studies/{id}/export`) and readable with `tessera parquet-schema`; the API runs the study and the page opens at the top with the IC heat table first (feature by horizon, with top-minus-bottom decile bps, breakeven cost, and t-stat on hover; rows sort by breakeven cost, |IC|, or name), then the charts as inline SVG in the terminal style: IC by horizon for every feature with the selected one bold (a point selects that cell), the decile bars and table of the selected cell, its costless equity curve (clipped z-score or sign position, taken every bar or rebalanced every horizon so no holds overlap; Sharpe, turnover, breakeven, with the rebalanced Sharpe and breakeven beside the per-bar figures in the ranked table), the per-day IC strip in the diagnostics, and a ranked table of every cell by breakeven cost. `web/scripts/chart-check.mjs` (`npm run chart-check`, part of `scripts/check.sh`) serves the built bundle with the studies API answered from `web/fixtures/study-result.json` (a 5-minute study of the bundled DEMO.US data) and fails unless every chart renders in both display modes without console errors, the page sits at the top, the grid precedes the charts, and the form on the daily grid hides the order-book checkboxes, pre-ticks the OHLCV set, offers the catalog picker, lists a checkbox per registered series with the lake feeds greyed out, and submits exactly that; a lake study submitted with funding_rate ticked must carry it.
- A deploy loop for the Mac mini: `scripts/deploy-local.sh` pulls `origin/main` fast-forward, rebuilds what the new commits touched (the engine for `src/`, Cargo, `build.rs`, or strategy changes; the bundle for `web/`), and restarts the service only for an engine change and only when no job or study is running, checking `data/ui/api.pid` against the port's listener and writing the new pid; nothing new means an immediate exit, a running job means a refusal that says so (exit 3), and `--dry-run` prints the plan. `--launchd` prints a LaunchAgent plist that runs it every five minutes; each run logs one line to `data/ui/deploy.log`. `--self-test` proves the decisions against a fixture origin and a stubbed service, and runs inside `scripts/check.sh`.
- Live progress for running jobs. The engine prints throttled `progress:` lines (data load, then replay by session), the API streams the worker log while the job runs and exposes the latest line as `progress` on running jobs, and the dashboard's worker-queue panel shows stage, percent, sessions done, elapsed time, and an estimate of time remaining; the status strip carries the percent too.
- Strategy-specific simple and advanced parameter editing, including costs-off research mode.
- A searchable instrument picker backed by `GET /api/instruments`, which indexes the configured catalog (`catalog.csv` plus universe lists and provider sub-catalogs) against the daily, five-minute, and one-minute files actually on disk and returns symbol, name, venue, asset class, currency, listing status, and first/last bar dates per resolution. The picker prevents duplicates, keeps priority order, flags selected symbols that lack data for the chosen resolution, and the API re-validates data presence before the run is queued. The resolved instruments are frozen in each run's `strategy_config.toml`.
- Immutable named presets stored in the local catalog.
- A controlled Sweeps & Comparisons workspace: one- or two-parameter Development grids over any numeric manifest parameter (up to 25 immutable configurations), live sensitivity heatmaps, stability-aware ranking, and normalized side-by-side comparison of up to four completed runs. Sweep ranking never reads Validation or Final Holdout results.
- An immutable Portfolio Builder backed by the engine's event-time capital model. It supports unconstrained full-size overlays, verified non-overlapping capital reuse, and normalized fixed allocations; source costs and lagged volatility targets stay frozen. Every saved portfolio includes an interactive report plus daily, weekly, and monthly return-correlation matrices.
- Per-run output folders and worker logs under `artifacts/ui_runs/`.
- Starred runs and cached run metrics. Each completed run caches CAGR, total return, Sharpe, Sortino, Calmar, max drawdown, annualized volatility, win rate, trade count, and the report window in the catalog (`metrics_json` on `runs`), computed when a job finishes and backfilled in the background at service start for older runs (legacy reports without parquet output fall back to `summary.json` when present, otherwise show no metrics). The strategy page's Historical runs table and the Runs list show those columns, sort by any of them, and filter by research label or starred-only. Any run can be starred from the list or its detail page via `POST /api/runs/{id}/star`; starring is a curation flag and never alters run artifacts. The Portfolio Builder's source list shows starred runs only by default, with a "Show all" toggle.
- Interactive structured run details: log-scale equity, metrics, coverage warnings, monthly and annual returns, long/short breakdown, trades in entry order with entry and exit timestamps and leverage at entry, and the frozen configuration.
- A run-coverage audit of selected signal sessions by symbol, year, and exact missing symbol-date, folded closed at the bottom of the Data page's Inventory view until it moves to the run's detail page. This is deliberately labeled signal-session coverage rather than full-calendar vendor coverage; the fold loads the newest completed structured run when opened.
- A centralized, immutable execution-cost library with fixed-tick/per-unit, all-in basis-point, and costs-off profiles. Entry and exit assumptions are stored separately, compatible global defaults can be overridden by an individual run, and the selected profile is frozen into the run manifest.
- A Data page of three views under one tab strip, the view last chosen remembered in browser storage and each opening at the top. `Inventory` (DS-06; decisions 0003, 0021, 0022) is the registered sources first: a card per source from `GET /api/sources` (header with name, kind, root, "token set, verified <time>", Replace token, Verify, Rescan, Remove; the connection state with its time and the root volume's used, free, and total; the credits line, used / limit, "resets 00:00 UTC", what is left above the reserve, and the reserve editable in place; the datasets table, dense under 1500 px, with exchange, types, resolution, from, folder, listed, on disk, latest, current, size, state, and a remove action; the Uncataloged line per folder with the scan's time, shown even for a source with no datasets, which says "no datasets yet, add one to scan"; Add dataset as an inline form fed by the cached availability), with Add source as an inline form on the panel (kind, name, root, catalog, token as a password field cleared after the save and never rendered again, reserve; root and catalog pre-filled from the configured library so the first EODHD source adopts it in place); then "Available from <source>" (the cached exchange list with type counts, resolutions, and the datasets registered against each exchange, "listed <time>" in its title, Refresh, a filter by code or name, rows with datasets first, and `+` on a row whose listing is not cached to fetch it; a select picks the source when there are several); then the configured library: every `local.toml` source (CSV bar library feeds, catalog and universe lists, freshness, update command; the parquet tick lake's feeds and instruments) with paths, file counts, sizes, and first/last dates, a Rescan button, and a Configure sources panel with the `local.toml` template and the steps for adding a source (see [DATA_SOURCES.md](DATA_SOURCES.md)); then the library metrics (latest market date, daily files on it, freshness from the optional `freshness_file` as a date and time in UTC, the last update job); then the run-coverage fold. A service from before the source API shows the cards panel with a line saying so rather than loading forever. `Instrument search` is one query box over `GET /api/instruments` listing symbol, name, venue, class, currency, listing status, and the EOD, 5m, 1m, and tick columns (the last bar date where the file was read, a tick where only the flag is known, a dash where the resolution is missing); a clicked row is the selection and shows every resolution's first and last date; the query, the rows, and the selection are app state, so they are still there after a visit to another workspace. `Updates & schedules` lists the update actions with their source, scope, state, and last run (the sources rescan; the library's `update_command` from `local.toml`, launched from there) over the local schedules for that command. Seeded schedules are disabled by default and execute only while the local service is running. `web/scripts/data-page-check.mjs` (`npm run data-page-check`, part of `scripts/check.sh`) serves the built bundle with `/api/data/sources`, `/api/data/status`, `/api/automations`, and `/api/instruments` answered from `web/fixtures/data-sources.json`, and `/api/sources` with its availability, dataset, token, scan, and reserve endpoints answered statefully from the fixture's two sources (one Credentials rejected; five datasets, an Uncataloged folder, the US and LSE availability rows, and a usage object on the first), and fails, in either display mode, unless each view opens at the top after the page was scrolled, Inventory's panels come in wireframe order (source cards, Available from, configured library, library metrics, the coverage fold closed) with each card's header, state, credits, datasets, and Uncataloged lines in that order, the rejected source showing Credentials rejected with the provider's message, the datasets table showing every column of the wireframe, the credits line reading used / limit and "resets 00:00 UTC", the availability title saying "listed <time>" with the rows with datasets first, the filter narrowing to LSE and its `+` posting a refresh for LSE alone, Add dataset posting exactly the exchange, types, resolution, from-date, folder, and include-delisted the form showed, Replace token and Add source (pre-filled with the configured library's root and catalog, the token a password field) posting the fixture's token once and no element's text or value carrying that token or the secrets path afterwards, the seeded Inventory fitting 1280 and 1440 px with no table wider than its wrapper, the library metrics showing the freshness time as a date and time rather than the raw ISO string, a query typed in Instrument search lists the fixture's matches under those ten columns with the clicked row selected and is still there with its rows and selection after opening Studies and coming back, Updates & schedules shows the update command's state and the fixture's schedules, and a fresh page opens the Data page on the view last chosen.
- Registered data sources (decisions 0020 and 0021): a provider account added from the console with no restart, one row of `data_sources` in the catalog and its API token in `data/ui/secrets/<id>.token` (mode 0600, in a 0700 folder). `GET /api/sources` lists the cards with the provider kinds compiled in; `POST /api/sources` (`kind`, `name`, `root`, `catalog_dir`, `token`, optional `reserve_pct`) verifies the token with the provider before anything is written and answers 422 with the provider's message for a rejected token (502 when the provider cannot be asked), nothing saved either way; `PUT /api/sources/{id}/token` replaces it under the same rule; `POST /api/sources/{id}/verify` re-checks the token on file and records the outcome; `DELETE /api/sources/{id}` answers 409 while any file lies under the source's root and otherwise removes the row and the token file, never a data file. A card carries name, kind, root and catalog folder, whether the root exists, the root volume's total, used, and free bytes (statvfs), `token_set` and `token_set_at`, `verified_at`, `verify_state` (`connected`, `credentials_rejected`, `unreachable`), and `verify_message`; no response, log line, or error message carries the token or the file's path. `TESSERA_EODHD_BASE_URL` points a scratch console's EODHD adapter at a stub server. See [DATA_SOURCES.md](DATA_SOURCES.md).
- Credits (decision 0022): each card's `usage` is what the provider last reported (requests today, daily limit, when the counter resets, when it was checked) with the source's reserve in calls and what a job may still spend. `GET /api/sources` refreshes it through the adapter unless the source was checked within the last minute (`?refresh=1` asks regardless, and so does a verify); with the provider unreachable the card keeps the last figures with their time and its state says so. `PUT /api/sources/{id}` sets `reserve_pct` (default 5, the share of the daily limit jobs leave untouched). The card's credits line renders used / limit, the reset time labelled "resets 00:00 UTC", and the reserve field; every job checks `src/provider/budget.rs` against these figures before it starts and refreshes them when it ends.
- The provider's availability, cached per source (decision 0013): `POST /api/sources/{id}/availability/refresh` fetches the exchange list and the active listings of every exchange with a dataset registered against the source (or of the one the body names, `{"exchange": "US"}`) into `provider_exchanges` and `provider_listings`, each row stamped with the time fetched, and answers 200 with the table whatever the provider said (409 with no token file, 400 for an exchange the provider does not list); `GET /api/sources/{id}/availability` serves the cached table: `fetched_at`, `refreshed_at`, an `unreachable` note when the last refresh failed (the rows fetched before stay, their times included), and per exchange its code, name, country, resolutions, `listings_fetched_at`, `listed`, and `types`, the listing counted per type under the provider's own type names, plus `delisted` and `delisted_fetched_at` once the delisted listing has been fetched (`"delisted": true` on the refresh body, or unasked where a dataset of the exchange includes delisted symbols). The tables go with their source when it is deleted.
- Datasets and the scan cache (decisions 0012, 0013, 0021): `POST /api/sources/{id}/datasets` registers what a source keeps current (exchange, types, resolution, from-date, folder, include delisted) against the cached availability, 400 for anything the cache does not list and 409 for a listing not cached yet or a dataset already covering a type there; `POST /api/sources/{id}/scan` runs the scan in the background (202; the card says `scanning` until it is done; 409 while one runs) and writes each dataset's listed, on disk, latest date, count current through the latest expected session, bytes, and BT-605 state, plus the Uncataloged folders under the root; `DELETE /api/datasets/{id}` is 409 while files exist for its listed symbols and otherwise answers 200 with the card. `GET /api/sources` serves each card's `datasets` with their last scan, `uncataloged`, `scanned_at`, and `scanning`; a second source over a root already registered is 409, and a source with datasets is guarded by its dataset folders rather than the whole root. The states are defined in [DATA_SOURCES.md](DATA_SOURCES.md).
- Localhost serving for every preserved original HTML report.
- Modern dark and Bloomberg-inspired terminal display modes. Terminal mode is the default and the choice is remembered in browser storage. It uses a command ribbon with a live clock, a bottom status strip (engine state, worker queue, report count, latest US EOD date, display mode, clock), amber function-title bars in place of page intros and hero copy, one monospace family for all text, 20px grid rows with 10px amber headers, outlined amber primary actions, blue text links, dark-amber editable fields, and equity charts drawn as a thin white line over a faint blue fill with dotted gridlines, right-hand equity axis labels, and an amber crosshair with a date / equity / drawdown readout. Dense grids carry units in the column header (CAGR %, Max DD %) rather than on each value. The terminal palette follows Bloomberg WEI, DES, HP, GP, BTMM, ECO, and OVME screens: black ground and black `#050505` panels with gray `#414141` lines, amber `#f89828` labels, tags, and identifiers, white `#f0f0f0` values, dark-red `#a00820` section bars with white titles, gray `#383838` column-header bars, green `#32c832` and red `#e03040` for signed values, blue `#6a9bff` text links, navy only as the row hover and selection highlight, and amber-filled editable fields with dark text that turn yellow on focus. In both display modes, form controls in every form grid are 42 px boxes with 18 px text under 15 px labels; each label packs caption, hint, and control to its bottom edge so a row's controls line up whatever the caption length. The form grids (`.field-grid`) are auto-fill tracks of 92 px, a six-character number box: a numeric field (`label.numeric`, the `type="number"` inputs) takes one track and a date, select, or text field two, so a row holds as many fields as fit the panel (six in the Configure run dialog's Data row at 1280 px) and a short row's numbers stay narrow instead of stretching. `web/scripts/theme-check.mjs` (`npm run theme-check`, part of `scripts/check.sh`) parses the stylesheet and fails on any blue background or border in terminal mode outside hover and selection rules, and on any control font below those sizes in either mode (the theme-neutral sizing block stays last in the stylesheet), and on any grid track that is a bare `fr` rather than `minmax(0, fr)`, since a content-sized track lets a wide table push the page past the viewport. Up to 1500 px wide (a 13-inch MacBook) the monthly heat map, the run history, the strategies catalog, and the study tables go dense (15 px cells, 13 px headers), run and strategy names truncate, the window column wraps, and the studies page stacks its history above the results, so nothing scrolls behind macOS's hidden scrollbars. `web/scripts/layout-check.mjs` (`npm run layout-check`, also in `scripts/check.sh`) serves the built bundle with the API proxied to the running console, opens the run overview, the strategies catalog (which must show its nine columns), a strategy page, the studies page, and the Data page's three views (Instrument search with a query the console's catalog answers) at 1280 and 1440 px in both modes, and fails when the page scrolls horizontally, an element passes the viewport, or a table is wider than its wrapper, and on the strategy page when the Historical runs table is not the first panel after the summary strip, when the Configure run dialog, opened at each width, passes the viewport, scrolls its rows inside itself, or leaves the page behind it scrolling horizontally, or when the first row of the dialog's Data grid holds fewer than six fields; it also adds an eight-parameter strategy to the catalog it serves (the fixture strategy with four more parameters, long captions and hints among them, two on the Advanced tier) and holds its dialog to the same rules with Simple showing and again with Advanced showing all eight; it skips with a message when no Chromium (playwright or patchright; each candidate is proven with a launch, so a playwright whose browsers were never downloaded is passed over) or no console is available, and lists a page the console cannot supply (no runs, no strategies) as skipped while it measures the rest. On CI the web job installs `playwright`'s Chromium (a web devDependency), builds the engine, and runs `scripts/scratch-console.sh start`, which serves a throwaway console from a scratch root under `target/` on the bundled example data with one completed run and one completed daily study seeded, so the layout and chart checks measure real pages there too; `TESSERA_ADDR` moves such an instance off port 8787 (`--port 8788` locally, then `LAYOUT_CONSOLE=http://127.0.0.1:8788/`). `web/scripts/run-form-check.mjs` (`npm run run-form-check`, also in `scripts/check.sh`) serves the bundle with the strategy API answered from `web/fixtures/strategy-detail.json` (the bundled RSI Mean Reversion strategy with a seeded preset and two synthetic runs), opens its page in both modes, applies the preset inside the dialog, closes the dialog with Escape, reads the strip, and fails unless the job the strip's Run posts and the job the dialog's Run posts both carry every value the strip shows and equal each other; it also holds the rules fold closed with its count on the bar, the dialog modal with its four rows, and Cancel keeping the edits.
- Readable desktop typography with 15px table bodies and 13px table headers.
- A strategy Code workspace that exposes the one-file Rust source for every runnable strategy, including strategies compiled in from private folders. Built-in and released source is read-only and SHA-256 identified; editable drafts live under `strategy_workspace/drafts/`. "New one-file strategy" writes the SDK skeleton into a draft, "Build & run" compiles a dev engine and registers a `<id>__dev` catalog row, and Release promotes it to an immutable version.
- Versioned source development from the browser: create a strategy from the skeleton or fork an existing one, edit its file, run Rust formatting, compile all targets, execute the library test suite, inspect the complete worker log, and promote a successfully tested draft to a new immutable catalog identity.
- Custom releases receive a dedicated compiled engine binary under `strategy_workspace/releases/`. They share the SDK run configuration and reporting plumbing, but execute the released source instead of the currently installed engine. New backtests preserve a source snapshot and hash in their artifact directory and run manifest.
- The native EOD download job (BT-1205, decisions 0020 and 0022): `POST /api/datasets/{id}/update` queues a daily dataset's update through the source's own adapter and answers 202 with the job record; the job fetches each session after the dataset's latest date in bulk (one call, plus one for the session's splits), refuses a day that omits the calendar symbol or is under the dataset's `min_bulk_rows` with nothing written, appends one row per file, refetches and replaces a split symbol's file so its adjusted closes stay on one basis, backfills the symbols with no file from the from-date until the source's reserve, regenerates `catalog.csv`, `stocks.txt`, and `etfs.txt` in the catalog folder from the cached listing, and writes every file as a part file renamed into place. One job runs per source at a time (a second is 409 naming it), a missing, unwritable, or unmounted folder is refused before any call and never created, the record (`dataset_jobs`: state with percent, started, finished, calls, files added and updated, symbols skipped with reasons, the estimate, error) is served by `GET /api/datasets/jobs/{id}` with its log as a download from `GET /api/datasets/jobs/{id}/log` (kept under `data/ui/logs/`), the dataset says `Updating` on its card meanwhile with the job as `last_job`, and the source's usage is refreshed and the source rescanned when the job ends. See [DATA_SOURCES.md](DATA_SOURCES.md).

The existing reports and strategy configurations are not modified. A newly queued run gets its own output directory and database record; the UI never reuses or overwrites an existing run directory.

The seeded cost profiles are:

- US equities: one adverse $0.01 tick plus $0.005/share on entry and exit.
- US equities: 10 bps all-in round trip.
- Spot FX: 4 bps all-in round trip.
- Crypto spot: 10 bps all-in round trip.
- Costs off: zero modeled execution cost for gross-alpha diagnostics.

Custom profiles are append-only. The current fixed-tick strategy adapters require symmetric entry/exit tick and per-unit assumptions and do not yet support a minimum commission; all-in basis-point profiles may store different entry and exit estimates because the engines consume their exact sum.

## One-file strategies (Strategy SDK)

A strategy is a single Rust file in `src/strategies/user/` that implements `crate::sdk::Strategy`: a `manifest()` declaring parameters (int, decimal, bool, choice with defaults, ranges, help, simple/advanced tier), a `new(params, symbol)` constructor, and `on_bar(ctx, bar)`. The context exposes the account (`equity`, `position`, `is_flat`) and accepts orders (`buy`, `buy_with` with stop/target, `sell_short`, `close`, `set_stop`) with platform sizing (`Size::Default`) or strategy overrides (`Size::Percent`, `Size::Units`). Streaming indicators (`Rsi`, `Sma`, `Ema`, `Atr`, `Bollinger`, `Vwap`, `RollingHigh`, `RollingLow`, `Crossover`) come from `crate::sdk::prelude`. `build.rs` discovers every file in that folder, so there is no module list or registry to edit; the reference file is `src/strategies/user/rsi_mean_reversion.rs` and the skeleton is `docs/templates/sdk_strategy_skeleton.rs`.

Strategies are resolution-agnostic. The run form chooses daily, five-minute, or one-minute bars, regular or extended hours, the symbol list (one strategy instance per symbol on a shared account), position size, and initial capital; manifest parameters render automatically. The engine exposes `tessera sdk-manifests` and `tessera run-strategy --config <toml>`; the local service syncs every compiled manifest into the catalog at startup (`base_strategy_id = "sdk"`, `command_name = "run-strategy"`).

The SDK also covers intraday strategies with daily context, resting limit orders, and universe-scale screens. A manifest can declare `daily_context()` (intraday runs receive completed daily bars through `on_daily_bar`), `screened_universe()` (a daily `screen()` pass across every symbol decides which symbol-days get intraday bars, so the full US common-stock universe runs in seconds), `allows_short()`, and `entry_limits(max_per_day, tie_break, seed)`. The context offers `buy_limit`/`sell_short_limit` with expiry, gap veto, percent stop, and timed exits; `exit_at`/`exit_after_minutes`; `priority()` for the daily entry cap; shared run-wide state (`shared_get/set/push/series`); and `symbol_index()` so list order can act as priority. The run form exposes a universe selector (all US common stocks, all ETFs, or both), entry limits (max entries per day, max open positions, priority / seeded-random / alphabetical tie-break), and accepts any cost profile including all-in basis points. Two production strategies ship as one-file SDK versions: `gap_fade` (any ETF list in priority order; first confirmed instrument trades, or largest gap) and `limit_buyer` (screened universe). Their legacy engines remain frozen for existing runs.

From the Code workspace, **New one-file strategy** writes a skeleton draft; **Build & run** compiles a dev engine for the draft, registers `<id>__dev` catalog entries that point at it, and opens the strategy page. **Release** still produces an immutable version with its own engine. Parameter sweeps are not yet available for SDK strategies.

## Strategy code workflow

Open **Code** in the sidebar or use **View source code** from a strategy page. A new strategy starts from one of the existing execution templates so it automatically has a valid data, parameter, cost, and reporting contract.

1. Create a draft from a released strategy.
2. Select and edit the Rust files in the draft bundle, then save each changed file.
3. Run **Format**, **Compile**, and **Run tests**. Validation runs in a separate local worker checkout and never edits the installed production source.
4. After a successful test against the current source hash, choose a new strategy id, name, and version and build the immutable release.
5. Open the newly registered strategy from the catalog and run backtests normally.

Changing any draft file invalidates its previous validation. Released strategies and source bundles cannot be overwritten through the UI. The initial editor deliberately supports strategy families already represented by the engine; adding a completely new data/execution adapter still requires extending the shared Rust plumbing.

## Development startup

Run the API from the repository root:

```sh
cargo run --bin tessera-ui
```

For UI development with hot reload, run the Vite dev server in a second terminal; it serves the
console on `http://127.0.0.1:5173/` and proxies `/api`, `/reports`, and `/artifacts` to the service:

```sh
cd web
npm run dev
```

`npm run build` writes the production bundle to `web/dist` (git-ignored), which the Rust service
serves; the launcher runs it when the bundle is missing or older than the source.

Keep the checkout out of iCloud-synced and macOS-guarded folders (Documents, Desktop); on the Mac mini it lives at `~/Code/Tessera`. If a checkout must live in such a folder, keep `web/node_modules` outside it: macOS evicts large files and Node tools stall for minutes while they re-download. The launcher creates `web/node_modules` as a symlink to `~/Library/Caches/Tessera/web-node_modules` for that reason; do the same by hand if you install with `npm ci` directly.

The service health check is `http://127.0.0.1:8787/api/health`. Runtime logs for the double-click launcher are written under `data/ui/logs/`.
